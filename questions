1. 统计dict.txt中，以a开头的单词有多少个，大小写都包括（a.../A...)
提示：以string形式，遍历每一行内容，string对象的startswith方法
运行结果要求在1秒以内

2.统计dict.txt中，每个字母，以这个字母开头的单词，各有多少个，输出结果例如：{'a': 801, 'b': 983...'z': 183}
提示：dict数据结构，以每个字母作为key
运行结果要求在1秒以内

3.找出最长的单词
4.哪个字母开头的单词最多？用程序找出来，结合题目2的结果
5.打印出第50-60行的
提示：把满足条件的数据，放到list中，一次都打印出来
